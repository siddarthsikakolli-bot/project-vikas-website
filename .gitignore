# macOS
.DS_Store
**/.DS_Store

# Design reference material (local only — large screenshots/video, not site source)
# Design reference material: large binaries, local input to the design
# process rather than site source. Pattern-matched rather than listed by
# name — naming them individually is how Ref_Outpost/ was committed by
# accident when it arrived later.
Ref_*/

# Node / build
node_modules/
dist/
.vercel/
*.log
.env
.env.local
.claude/settings.local.json

# Astro
.astro/

# Full-size photo originals — kept locally, web versions live in public/media
originals/
